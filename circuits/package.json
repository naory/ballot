{
  "name": "@ballot/circuits",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "install-deps": "npm install",
    "compile": "bash scripts/compile.sh",
    "setup": "bash scripts/setup.sh",
    "test": "PATH=$PATH:$HOME/.cargo/bin mocha --timeout 60000 'test/**/*.test.js'"
  },
  "dependencies": {
    "circomlib": "^2.0.5"
  },
  "devDependencies": {
    "circom_tester": "^0.0.24",
    "mocha": "^11.7.5",
    "poseidon-lite": "^0.3.0"
  }
}
